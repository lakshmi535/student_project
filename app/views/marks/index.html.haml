%table.table
  %thead
    %tr
      %th{:scope => "col"} #
      %th{:scope => "col"} Exam 
      %th{:scope => "col"} student Name
      %th{:scope => "col"} level Name
      %th{:scope => "col"} Maths
      %th{:scope => "col"} Telugu
      %th{:scope => "col"} Hindi
     
      / %th{:scope => "col"} exam_id
      / %th{:scope => "col"} avatar

  %tbody
    - @marks.each do |c|
      %tr
        %th{:scope => "row"}
        %td= Exam.find_by(id:c.exam_id).exam_name      
        %td= Student.find_by(id:c.student_id).name
        %td= Level.find_by(id:c.level_id).level_number
        %td= c.maths_marks
        %td= c.telugu_marks
        %td= c.hindi_marks
        %td= link_to 'Destroy', mark_path(c),method: :delete,data: { confirm: 'Are you sure?' }
        / %td= c.exam_id
        / %td= image_tag c.avatar.url(:thumb)
= link_to 'Back', students_list_path        